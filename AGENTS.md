# Repository Guidelines

- Preferred language: **Python 3**.
- Keep code readable with descriptive variable names and comments.
- Include docstrings for all public functions.
- When adding Python modules, ensure they can run with Python's standard library and `numpy`.
- If a `requirements.txt` or `tests` directory appears, run `pytest` after each commit.
- Commit messages should be concise yet descriptive.
